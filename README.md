# Тестовий проект для перевірки prettier

Це тестовий проект, в якому можна перевірити роботу git pre-commit hook, та подивитись як воно налаштовується

Для початку:

```bash
npm install
```

[Інструкція з налаштування hook](config-prettier.md)
